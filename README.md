# How to convert ts file to js for submission

Run command:

```bash
deno bundle file.ts file.js
```

Copy js content into new file and merge with read_line script to read the input and output the result
